# Simple PHP RDBMS

A minimal relational database management system built from scratch in PHP.

## Features
- SQL-like interface
- File-based storage
- CRUD operations
- Interactive CLI (REPL)
- Demo web app using the database

## How to Run
```bash
php cli/rdbms.php
